node_modules
.next
.turbo
dist
build
coverage
.DS_Store
*.log
.env
.env.local
.env.*.local
# Root Cause vs Logic: GitHub-mode clones are cached under `.cache/github-repos`, and without
# ignoring the cache root a user can accidentally `git add` an embedded checkout into the app repo.
.cache/
.agentdiagram-cache
playwright-report
test-results
tests/visual/__snapshots__/*.actual.png
tests/visual/__snapshots__/*.diff.png

.claude
.cursor
.vscode
.cache/github-repos/
